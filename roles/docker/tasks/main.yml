#SPDX-License-Identifier: MIT-0
---
# tasks file for roles/docker
#SPDX-License-Identifier: MIT-0
---
- name: Install prerequisites
  apt:
    name:
      - ca-certificates
      - curl
      - gnupg
    state: present
    update_cache: yes
  become: true

- name: Add Docker’s official GPG key
  ansible.builtin.file:
    path: /etc/apt/keyrings
    state: directory
    mode: "0755"
  become: true

- name: Install Docker apt key
  ansible.builtin.get_url:
    url: https://download.docker.com/linux/debian/gpg
    dest: /etc/apt/keyrings/docker.asc
    mode: "0644"
  become: true

- name: Add Docker apt repository
  ansible.builtin.copy:
    dest: /etc/apt/sources.list.d/docker.list
    mode: "0644"
    content: |
      deb [arch={{ lookup('pipe','dpkg --print-architecture') }} signed-by=/etc/apt/keyrings/docker.asc] https://download.docker.com/linux/debian {{ lookup('pipe', '. /etc/os-release && echo $VERSION_CODENAME') }} stable
  become: true

- name: Install Docker Engine
  apt:
    name:
      - docker-ce
      - docker-ce-cli
      - containerd.io
      - docker-buildx-plugin
      - docker-compose-plugin
    state: present
    update_cache: yes
  become: true

- name: Configure Docker daemon
  ansible.builtin.copy:
    dest: /etc/docker/daemon.json
    content: |
      {
        "log-driver": "json-file",
        "log-opts": { "max-size": "10m", "max-file": "3" },
        "exec-opts": ["native.cgroupdriver=systemd"]
      }
    owner: root
    group: root
    mode: "0644"
  notify: Restart docker
  become: true

- name: Ensure docker service enabled and running
  ansible.builtin.systemd:
    name: docker
    enabled: true
    state: started
  become: true