# Collections Plugins Directory

This collection currently ships role-focused automation and does not yet include custom plugins.

If custom plugins are added later, place them in subdirectories by plugin type, for example:

```
└── plugins
    ├── modules
    ├── module_utils
    ├── filter
    ├── inventory
    ├── lookup
    └── callback
```
